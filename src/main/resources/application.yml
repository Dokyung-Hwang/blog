spring:
  application:
    name: blog
  datasource:
    url: jdbc:mysql://localhost:3306/post?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul
    username: smiledk
    password: 1112
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    database: mysql
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
    open-in-view: false

logging:
  level:
    org:
      hibernate:
        type:
          descriptor: trace


